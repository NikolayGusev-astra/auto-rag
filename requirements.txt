# Unified dependencies now live in pyproject.toml (PEP 621).
# Install with the extras you need, e.g.:
#   pip install -e ".[dev,web,chroma,memory,reranker]"
#
# This file is a thin alias for backwards compatibility only.

-r pyproject.toml

# NOTE: chromadb / sentence-transformers / memvid-sdk are OPTIONAL extras
# (chroma / reranker / memory). They are intentionally NOT in the base
# dependency set to keep the minimal local-first install lean.
